import 'package:flutter/material.dart';

// define color template
const lightColor = Color(0XFFFFFFFF);

// define font size template
double largeHeading = 32.0;
double mediumHeading = 22.0;
double smallHeading = 18.0;
double body = 16.0;
double smallBody = 14.0;

// define space or margin between elements
Widget heightSpace(space) => SizedBox(
  height: space,
);
